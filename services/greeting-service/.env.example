# greeting-service
# ----------------
# Read via application.properties:
#   user.service.url=${USER_SERVICE_URL:http://localhost:8081}

# Local docker run / docker-compose value:
USER_SERVICE_URL=http://localhost:8081

# In-cluster value (set this via your ConfigMap):
# USER_SERVICE_URL=http://user-service.mini-mesh.svc.cluster.local:8080

# SERVER_PORT=8080
